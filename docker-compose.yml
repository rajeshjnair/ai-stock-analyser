version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: ai-stock-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    networks:
      - frontend
      - backend
    depends_on:
      redis:
        condition: service_healthy
      mongodb:
        condition: service_healthy
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: ai-stock-redis
    command: >
      redis-server
      --appendonly yes
      --maxmemory 256mb
      --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - backend
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  mongodb:
    image: mongo:7
    container_name: ai-stock-mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
      MONGO_INITDB_DATABASE: ai_stock_analyzer
    volumes:
      - mongo_data:/data/db
      - ./mongo-init:/docker-entrypoint-initdb.d:ro
    networks:
      - backend
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: unless-stopped

  # Placeholder for frontend app service
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: ai-stock-frontend
  #   networks:
  #     - frontend
  #   depends_on:
  #     - nginx
  #   restart: unless-stopped

  # Placeholder for backend API service
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: ai-stock-backend
  #   environment:
  #     - NODE_ENV=${NODE_ENV}
  #     - MONGO_URL=mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongodb:27017/ai_stock_analyzer?authSource=admin
  #     - REDIS_URL=${REDIS_URL}
  #     - FIREBASE_PROJECT_ID=${FIREBASE_PROJECT_ID}
  #     - FIREBASE_API_KEY=${FIREBASE_API_KEY}
  #     - FIREBASE_AUTH_DOMAIN=${FIREBASE_AUTH_DOMAIN}
  #     - FINNHUB_API_KEY=${FINNHUB_API_KEY}
  #     - GEMINI_API_KEY=${GEMINI_API_KEY}
  #     - JWT_SECRET=${JWT_SECRET}
  #   networks:
  #     - backend
  #     - frontend
  #   depends_on:
  #     - redis
  #     - mongodb
  #   restart: unless-stopped

  # Placeholder for WebSocket service
  # websocket:
  #   build:
  #     context: ./websocket
  #     dockerfile: Dockerfile
  #   container_name: ai-stock-websocket
  #   environment:
  #     - NODE_ENV=${NODE_ENV}
  #     - REDIS_URL=${REDIS_URL}
  #   networks:
  #     - backend
  #     - frontend
  #   depends_on:
  #     - redis
  #   restart: unless-stopped

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge

volumes:
  redis_data:
    driver: local
  mongo_data:
    driver: local
